name: openrouter
version: 1.0.0
description: "OpenRouter image generation backend. Uses FLUX-schnell by default via openrouter.ai/api/v1/images/generations."
author: braisntext
kind: backend
requires_env:
  - OPENROUTER_API_KEY

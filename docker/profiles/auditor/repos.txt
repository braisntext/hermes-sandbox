# Repos cloned into workspace/ at every container boot.
# Format: one "owner/repo" per line. Blank lines and # comments are ignored.
# Requires GITHUB_TOKEN to be set (configured automatically by 03-biglobster-config).
#
# Pilot: the auditor reviews the Hermes engine repo only. Phase 4 adds the
# profile repos (biglobster, FinView, grow-shop-*, SocialAgenda) here.
braisntext/hermes-sandbox
